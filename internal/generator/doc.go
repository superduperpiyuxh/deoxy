// Package generator is the top-level orchestrator that drives the deoxy
// pipeline: scan files, parse ASTs with tree-sitter, render doc comments
// via templates, and write them back to source files.
//
// Full implementation begins in Phase 3 (CLI integration).
package generator
